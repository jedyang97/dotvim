Installation:

    git clone git://github.com/jedyang97/dotvim.git ~/.vim
    git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

Open up vim and in normal mode, type:

    :source %
    :PluginInstall
